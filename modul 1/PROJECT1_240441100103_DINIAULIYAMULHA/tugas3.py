#soal nomor 3
#Input jumlah USD dan kurs dari pengguna
jumlah_usd = float(35)
jumlah_idr = float(input("masukkan kurs tanggal 25"))

# Hitung nominal dalam IDR
nominal_idr = (jumlah_usd * jumlah_idr)

# Tampilkan hasil
print("Nominal uang yang didapatkan Suraji dalam IDR:" , float(nominal_idr) , "IDR")